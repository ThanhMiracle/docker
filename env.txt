# Database
POSTGRES_PASSWORD=strongpassword123   # đổi theo ý bạn

# JWT
JWT_SECRET=myjwtsecretkey
JWT_EXPIRE_MINUTES=120

# MinIO
MINIO_ACCESS_KEY=minioadmin          # hoặc bất kỳ chuỗi nào (8 ký tự trở lên)
MINIO_SECRET_KEY=minioadmin123

# Backend
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/mydb

# Public URL cho MinIO (dùng để frontend load ảnh)
# Nếu bạn đang chạy trên máy local thì dùng máy host IP hoặc localhost
MINIO_PUBLIC_URL=http://localhost/minio-public

# Frontend gọi API bằng path để tránh hardcode IP
VITE_API_BASE=/api